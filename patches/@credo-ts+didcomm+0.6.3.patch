diff --git a/node_modules/@credo-ts/didcomm/build/DidCommDispatcher.mjs b/node_modules/@credo-ts/didcomm/build/DidCommDispatcher.mjs
index e1a0c77..804363d 100644
--- a/node_modules/@credo-ts/didcomm/build/DidCommDispatcher.mjs
+++ b/node_modules/@credo-ts/didcomm/build/DidCommDispatcher.mjs
@@ -29,6 +29,16 @@ let DidCommDispatcher = class DidCommDispatcher {
 			if (!messageHandler) throw new DidCommProblemReportError(`Error handling message ${inboundMessageContext.message.id} with type ${inboundMessageContext.message.type}. The message type is not supported`, { problemCode: DidCommProblemReportReason.MessageParseFailure });
 			const outboundMessage = await messageHandler.handle(inboundMessageContext);
 			if (outboundMessage) inboundMessageContext.setResponseMessage(outboundMessage);
+
+			this.eventEmitter.emit(inboundMessageContext.agentContext, {
+			type: DidCommEventTypes.DidCommMessageProcessed,
+			payload: {
+				message: inboundMessageContext.message,
+				connection: inboundMessageContext.connection,
+				receivedAt: inboundMessageContext.receivedAt,
+				encryptedMessage: inboundMessageContext.encryptedMessage
+			}
+			});
 			await next();
 		};
 		this.messageSender = messageSender;
@@ -73,15 +83,6 @@ let DidCommDispatcher = class DidCommDispatcher {
 			if (!outboundMessage.inboundMessageContext) outboundMessage.inboundMessageContext = messageContext;
 			await this.messageSender.sendMessage(outboundMessage);
 		}
-		this.eventEmitter.emit(agentContext, {
-			type: DidCommEventTypes.DidCommMessageProcessed,
-			payload: {
-				message,
-				connection,
-				receivedAt: messageContext.receivedAt,
-				encryptedMessage
-			}
-		});
 	}
 };
 DidCommDispatcher = __decorate([
diff --git a/node_modules/@credo-ts/didcomm/build/DidCommMessageSender.mjs b/node_modules/@credo-ts/didcomm/build/DidCommMessageSender.mjs
index aedda59..6d1f81a 100644
--- a/node_modules/@credo-ts/didcomm/build/DidCommMessageSender.mjs
+++ b/node_modules/@credo-ts/didcomm/build/DidCommMessageSender.mjs
@@ -296,11 +296,13 @@ let DidCommMessageSender = class DidCommMessageSender {
 			const serviceSchema = utils.getProtocolScheme(service.serviceEndpoint);
 			return transportPriority.schemes.includes(serviceSchema);
 		});
-		if (transportPriority?.schemes) services = services.sort((a, b) => {
-			const aScheme = utils.getProtocolScheme(a.serviceEndpoint);
-			const bScheme = utils.getProtocolScheme(b.serviceEndpoint);
-			return transportPriority?.schemes.indexOf(aScheme) - transportPriority?.schemes.indexOf(bScheme);
-		});
+		if (transportPriority?.schemes) {
+			const schemePriority = (endpoint) => {
+				const index = transportPriority.schemes.indexOf(utils.getProtocolScheme(endpoint));
+				return index === -1 ? Number.MAX_SAFE_INTEGER : index;
+			};
+			services = services.sort((a, b) => schemePriority(a.serviceEndpoint) - schemePriority(b.serviceEndpoint));
+		}
 		agentContext.config.logger.debug(`Retrieved ${services.length} services for message to connection '${connection.id}'(${connection.theirLabel})'`, {
 			hasQueueService: queueService !== void 0,
 			transportPriority
diff --git a/node_modules/@credo-ts/didcomm/build/modules/routing/DidCommMediationRecipientApi.mjs b/node_modules/@credo-ts/didcomm/build/modules/routing/DidCommMediationRecipientApi.mjs
index 1f4452a..0b981c1 100644
--- a/node_modules/@credo-ts/didcomm/build/modules/routing/DidCommMediationRecipientApi.mjs
+++ b/node_modules/@credo-ts/didcomm/build/modules/routing/DidCommMediationRecipientApi.mjs
@@ -151,18 +151,27 @@ let DidCommMediationRecipientApi = class DidCommMediationRecipientApi {
 				});
 			}
 			case DidCommMediatorPickupStrategy.PickUpV2LiveMode:
-				this.logger.info(`Starting Live Mode pickup of messages from mediator '${mediatorRecord.id}'`);
-				await this.monitorMediatorWebSocketEvents(mediatorRecord, mediatorPickupStrategy);
-				await this.messagePickupApi.pickupMessages({
-					connectionId: mediatorConnection.id,
-					protocolVersion: "v2",
-					awaitCompletion: true
-				});
-				await this.messagePickupApi.setLiveDeliveryMode({
-					connectionId: mediatorConnection.id,
-					liveDelivery: true,
-					protocolVersion: "v2"
-				});
+				try {
+					this.logger.info(`Starting Live Mode pickup of messages from mediator '${mediatorRecord.id}'`);
+					await this.monitorMediatorWebSocketEvents(mediatorRecord, mediatorPickupStrategy);
+					await this.messagePickupApi.pickupMessages({
+						connectionId: mediatorConnection.id,
+						protocolVersion: "v2",
+						awaitCompletion: true
+					});
+					await this.messagePickupApi.setLiveDeliveryMode({
+						connectionId: mediatorConnection.id,
+						liveDelivery: true,
+						protocolVersion: "v2"
+					});
+				} catch (error) {
+					this.logger.warn('Unable to set live delivery mode for mediator', { error });
+					// Make sure the outbound WebSocket is closed, so the close event triggers reconnection
+					const wsTransport = this.agentContext.dependencyManager.resolve(DidCommModuleConfig).outboundTransports.find(t => t.supportedSchemes?.includes('ws'));
+					if (wsTransport && typeof wsTransport.closeSocketByConnectionId === 'function') {
+						wsTransport.closeSocketByConnectionId(mediatorConnection.id);
+					}
+				}
 				break;
 			case DidCommMediatorPickupStrategy.Implicit:
 				this.logger.info(`Starting implicit pickup of messages from mediator '${mediatorRecord.id}'`);
