diff --git a/node_modules/@credo-ts/core/build/agent/Agent.js b/node_modules/@credo-ts/core/build/agent/Agent.js
index 2db29e1..0bb1be0 100644
--- a/node_modules/@credo-ts/core/build/agent/Agent.js
+++ b/node_modules/@credo-ts/core/build/agent/Agent.js
@@ -113,6 +113,7 @@ class Agent extends BaseAgent_1.BaseAgent {
             connection: e.payload.connection,
             contextCorrelationId: e.payload.contextCorrelationId,
             session: e.payload.session,
+            receivedAt: e.payload.receivedAt,
         })
             .catch((error) => {
             this.logger.error('Failed to process message', { error });
diff --git a/node_modules/@credo-ts/core/build/agent/Dispatcher.js b/node_modules/@credo-ts/core/build/agent/Dispatcher.js
index 6374d4d..478d869 100644
--- a/node_modules/@credo-ts/core/build/agent/Dispatcher.js
+++ b/node_modules/@credo-ts/core/build/agent/Dispatcher.js
@@ -44,6 +44,16 @@ let Dispatcher = class Dispatcher {
             if (outboundMessage) {
                 inboundMessageContext.setResponseMessage(outboundMessage);
             }
+            // Emit event that allows to hook into received messages
+            this.eventEmitter.emit(inboundMessageContext.agentContext, {
+                type: Events_1.AgentEventTypes.AgentMessageProcessed,
+                payload: {
+                    message: inboundMessageContext.message,
+                    connection: inboundMessageContext.connection,
+                    receivedAt: inboundMessageContext.receivedAt,
+                    encryptedMessage: inboundMessageContext.encryptedMessage,
+                },
+            });
             await next();
         };
         this.messageSender = messageSender;
@@ -109,16 +119,6 @@ let Dispatcher = class Dispatcher {
             }
             await this.messageSender.sendMessage(outboundMessage);
         }
-        // Emit event that allows to hook into received messages
-        this.eventEmitter.emit(agentContext, {
-            type: Events_1.AgentEventTypes.AgentMessageProcessed,
-            payload: {
-                message,
-                connection,
-                receivedAt: messageContext.receivedAt,
-                encryptedMessage,
-            },
-        });
     }
 };
 exports.Dispatcher = Dispatcher;
diff --git a/node_modules/@credo-ts/core/build/modules/routing/MediationRecipientApi.js b/node_modules/@credo-ts/core/build/modules/routing/MediationRecipientApi.js
index fffd69f..e62b949 100644
--- a/node_modules/@credo-ts/core/build/modules/routing/MediationRecipientApi.js
+++ b/node_modules/@credo-ts/core/build/modules/routing/MediationRecipientApi.js
@@ -25,6 +25,7 @@ const constants_1 = require("../../constants");
 const error_1 = require("../../error");
 const plugins_1 = require("../../plugins");
 const transport_1 = require("../../transport");
+const TransportService_1 = require("../../agent/TransportService");
 const ConnectionMetadataTypes_1 = require("../connections/repository/ConnectionMetadataTypes");
 const services_1 = require("../connections/services");
 const dids_1 = require("../dids");
@@ -183,6 +184,9 @@ let MediationRecipientApi = class MediationRecipientApi {
                 }
                 catch (error) {
                     this.logger.warn('Unable to re-open websocket connection to mediator', { error });
+                    // Make sure the WebSocket is closed, since the error could be generated by an unsuccesful delivery of set live mode message
+                    const session = this.agentContext.dependencyManager.resolve(TransportService_1.TransportService).findSessionByConnectionId(mediator.connectionId)
+                    if (session) await session.close()
                 }
             },
             complete: () => this.logger.info(`Stopping pickup of messages from mediator '${mediator.id}'`),
@@ -227,6 +231,7 @@ let MediationRecipientApi = class MediationRecipientApi {
                 return subscription;
             }
             case MediatorPickupStrategy_1.MediatorPickupStrategy.PickUpV2LiveMode:
+                try {
                 // PickUp V2 in Live Mode will retrieve queued messages and then set up live delivery mode
                 this.logger.info(`Starting Live Mode pickup of messages from mediator '${mediatorRecord.id}'`);
                 await this.monitorMediatorWebSocketEvents(mediatorRecord, mediatorPickupStrategy);
@@ -240,7 +245,14 @@ let MediationRecipientApi = class MediationRecipientApi {
                     liveDelivery: true,
                     protocolVersion: 'v2',
                 });
-                break;
+                } catch (error) {
+                    this.logger.warn('Unable to re-open websocket connection to mediator', { error });
+                    // Make sure the WebSocket is closed, since the error could be generated by an unsuccessful delivery of set live mode message
+                    const session = this.agentContext.dependencyManager.resolve(TransportService_1.TransportService).findSessionByConnectionId(mediatorConnection.id)
+                    if (session) await session.close()
+                }
+            break;
+
             case MediatorPickupStrategy_1.MediatorPickupStrategy.Implicit:
                 // Implicit means sending ping once and keeping connection open. This requires a long-lived transport
                 // such as WebSockets to work
