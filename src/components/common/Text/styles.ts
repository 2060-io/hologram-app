import { StyleSheet } from 'react-native'

import { redColor } from '@2060/constants'

export default StyleSheet.create({
  container: {
    fontSize: 20,
  },
  error: {
    color: redColor,
  },
})
