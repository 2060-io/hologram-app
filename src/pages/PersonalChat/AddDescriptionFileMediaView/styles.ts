import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  image: {
    width: '100%',
    height: '100%',
    borderRadius: 0,
    margin: 0,
    resizeMode: 'contain',
  },
  containerBgClose: {
    marginLeft: 10,
    alignSelf: 'flex-start',
  },
  btnClose: {
    borderRadius: 50,
  },
})
